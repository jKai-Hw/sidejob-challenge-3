---
description: コーディング規約（命名、インデント、コメント）
globs:
  - "**/*.php"
  - "**/*.scss"
  - "**/*.js"
  - "**/*.html"
alwaysApply: false
---

# コーディング規約

> 一貫性のあるコードを書くための詳細なルール

## 🎯 基本原則

- **可読性優先**: 他の人（将来の自分含む）が読んでも理解できるコード
- **一貫性**: プロジェクト全体で同じスタイルを維持
- **シンプル**: 複雑なロジックより、わかりやすいコード

---

## 📐 インデント・フォーマット

### 共通ルール
| 項目 | 設定 |
|------|------|
| インデント | **スペース2つ** |
| 行末スペース | 削除 |
| ファイル末尾 | 空行1つ |
| 最大行長 | 120文字（目安） |

### PHP
```php
<?php
// インデント: スペース2つ
function get_custom_data() {
  $result = array();
  
  if ( $condition ) {
    // 処理
  }
  
  return $result;
}
```

### SCSS
```scss
// インデント: スペース2つ
.component {
  display: flex;
  
  &__element {
    padding: $spacing-md;
    
    @include sp {
      padding: $spacing-sm;
    }
  }
}
```

### JavaScript/jQuery
```javascript
// インデント: スペース2つ
jQuery(function($) {
  'use strict';
  
  const initSlider = () => {
    // 処理
  };
  
  initSlider();
});
```

### HTML（ブロックパターン）
```html
<!-- インデント: スペース2つ -->
<!-- wp:group {"className":"section"} -->
<div class="wp-block-group section">
  <!-- wp:heading -->
  <h2>見出し</h2>
  <!-- /wp:heading -->
</div>
<!-- /wp:group -->
```

---

## 📛 命名規則

### ファイル名
| 種類 | 形式 | 例 |
|------|------|-----|
| SCSS | `_kebab-case.scss` | `_hero-section.scss` |
| PHP | `kebab-case.php` | `custom-post-types.php` |
| JS | `kebab-case.js` | `slider-init.js` |
| パターン | `kebab-case.html` | `hero-main.html` |
| テンプレート | `snake_case.html` | `single-service.html` |

### CSS/SCSSクラス名（BEM記法）
```scss
// Block: 独立したコンポーネント
.card { }

// Element: ブロックを構成する要素（__で接続）
.card__image { }
.card__title { }
.card__content { }

// Modifier: バリエーション（--で接続）
.card--featured { }
.card--horizontal { }

// ユーティリティクラス（u-接頭辞）
.u-text-center { }
.u-mt-lg { }

// JavaScript用クラス（js-接頭辞）
.js-slider { }
.js-toggle-menu { }
```

### SCSS変数名
```scss
// カラー: $color-{用途}
$color-primary: #3B82F6;
$color-text: #1F2937;
$color-background: #FFFFFF;

// フォント: $font-{プロパティ}-{サイズ/用途}
$font-family-base: 'Noto Sans JP', sans-serif;
$font-size-base: 1rem;
$font-weight-bold: 700;

// スペーシング: $spacing-{サイズ}
$spacing-xs: 0.5rem;
$spacing-sm: 1rem;

// ブレークポイント: $breakpoint-{サイズ}
$breakpoint-md: 768px;

// z-index: $z-{要素名}
$z-header: 100;
$z-modal: 1000;
```

### PHP関数名・変数名
```php
// 関数: snake_case（プレフィックス付き推奨）
function theme_get_featured_posts() { }
function theme_register_custom_post_types() { }

// 変数: snake_case
$post_id = get_the_ID();
$featured_image = get_the_post_thumbnail_url();

// 定数: UPPER_SNAKE_CASE
define( 'THEME_VERSION', '1.0.0' );

// クラス: PascalCase
class Theme_Custom_Walker extends Walker_Nav_Menu { }
```

### JavaScript変数・関数名
```javascript
// 変数・関数: camelCase
const menuToggle = document.querySelector('.js-menu-toggle');
function initSlider() { }

// 定数: UPPER_SNAKE_CASE
const API_ENDPOINT = '/wp-json/wp/v2/posts';

// jQueryオブジェクト: $接頭辞
const $slider = $('.js-slider');
const $menuButton = $('.js-menu-toggle');
```

---

## 💬 コメントルール

### SCSS
```scss
// ============================================
// セクション区切り（大見出し）
// ============================================

// -----------------------------------------
// サブセクション（中見出し）
// -----------------------------------------

// 単一行コメント（説明）

.component {
  // プロパティの説明（必要に応じて）
  width: calc(100% - 40px); // 左右20pxずつの余白を引く
}

// TODO: あとで対応する内容
// FIXME: 修正が必要な内容
// NOTE: 重要な補足情報
```

### PHP
```php
<?php
/**
 * ファイルの説明
 *
 * @package ThemeName
 * @since 1.0.0
 */

/**
 * 関数の説明
 *
 * @param int    $post_id 投稿ID
 * @param string $size    画像サイズ
 * @return string|false 画像URL、なければfalse
 */
function theme_get_thumbnail_url( $post_id, $size = 'large' ) {
  // 処理の説明
  return get_the_post_thumbnail_url( $post_id, $size );
}

// インラインコメント（短い説明）
$result = some_function(); // 結果を取得
```

### JavaScript
```javascript
/**
 * ファイルの説明
 *
 * @file スライダー初期化スクリプト
 */

/**
 * スライダーを初期化する
 * @param {string} selector - 対象要素のセレクタ
 * @param {Object} options - オプション設定
 */
function initSlider(selector, options = {}) {
  // 処理
}

// TODO: 実装予定
// FIXME: バグ修正必要
```

### HTMLコメント（ブロックパターン）
```html
<?php
/**
 * Title: ヒーローセクション
 * Slug: theme-name/hero-main
 * Categories: featured
 * Description: メインビジュアルのヒーローセクション
 */
?>

<!-- ヒーローセクション開始 -->
<!-- wp:group {"className":"hero"} -->
<div class="wp-block-group hero">
  <!-- メインビジュアル -->
  <!-- wp:image -->
  <!-- /wp:image -->
</div>
<!-- /wp:group -->
<!-- ヒーローセクション終了 -->
```

---

## 🚫 アンチパターン（避けるべきこと）

### 命名
```scss
// ❌ 悪い例
.btn1 { }           // 意味のない数字
.redButton { }      // 見た目を名前に（色が変わったら？）
.l-header { }       // 省略しすぎ
.header_main { }    // ケバブケースとスネークケースの混在

// ✅ 良い例
.button--primary { }
.header__main { }
```

### CSS
```scss
// ❌ 悪い例
#header { }           // IDセレクタ（詳細度が高すぎる）
div.card { }          // 不要な要素セレクタ
.card .card-title { } // ネストが深い（BEM使う）
!important           // 極力避ける

// ✅ 良い例
.header { }
.card { }
.card__title { }
```

### PHP
```php
// ❌ 悪い例
function fn1() { }              // 意味のない名前
extract( $_GET );               // セキュリティリスク
echo '<div>' . $var . '</div>'; // エスケープなし

// ✅ 良い例
function theme_get_featured_posts() { }
$page = isset( $_GET['page'] ) ? sanitize_text_field( $_GET['page'] ) : '';
echo '<div>' . esc_html( $var ) . '</div>';
```

---

## ✅ コード品質チェック

コミット前に確認：
- [ ] インデントは統一されているか（スペース2つ）
- [ ] 命名規則に従っているか
- [ ] 不要なコメントアウトは削除したか
- [ ] console.log / var_dump は削除したか
- [ ] 適切なコメントがあるか
