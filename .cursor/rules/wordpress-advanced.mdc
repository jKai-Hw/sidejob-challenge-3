---
description: カスタム投稿タイプ・ACF・動的要素の実装ルール
globs:
  - "wp-theme/**/*.php"
  - "wp-theme/**/templates/*.html"
  - "wp-theme/**/patterns/*.html"
alwaysApply: true
---

# WordPress拡張機能実装ガイドライン

> カスタム投稿タイプ、ACF、動的要素の実装ルール

## 🎯 対象サイト想定

- **規模**: 企業サイト 5〜10ページ
- **ページ構成**: TOP, About, Service一覧/詳細, News一覧/詳細, Contact
- **動的要素**: スライダー、絞り込み検索、お問い合わせフォーム、ACF入稿

---

## 📁 ページ構成とテンプレート

### テンプレート階層
```
templates/
├── front-page.html      # トップページ
├── page-about.html      # 会社概要
├── page-contact.html    # お問い合わせ
├── archive-service.html # サービス一覧
├── single-service.html  # サービス詳細
├── archive-news.html    # ニュース一覧
├── single-news.html     # ニュース詳細（または単体post使用）
├── page.html            # 汎用固定ページ
├── single.html          # 汎用投稿詳細
├── archive.html         # 汎用アーカイブ
├── 404.html             # 404エラー
└── index.html           # フォールバック
```

---

## 📦 カスタム投稿タイプ（CPT）

### 登録ファイル構成
```
wp-theme/
├── inc/
│   ├── custom-post-types.php   # CPT登録
│   ├── custom-taxonomies.php   # タクソノミー登録
│   └── acf-fields.php          # ACFフィールドグループ（コード管理する場合）
└── functions.php               # inc/をrequire
```

### CPT登録テンプレート（custom-post-types.php）
```php
<?php
/**
 * カスタム投稿タイプの登録
 *
 * @package ThemeName
 */

if ( ! defined( 'ABSPATH' ) ) {
  exit;
}

/**
 * サービス投稿タイプを登録
 */
function theme_register_service_post_type() {
  $labels = array(
    'name'                  => 'サービス',
    'singular_name'         => 'サービス',
    'menu_name'             => 'サービス',
    'add_new'               => '新規追加',
    'add_new_item'          => '新規サービスを追加',
    'edit_item'             => 'サービスを編集',
    'new_item'              => '新規サービス',
    'view_item'             => 'サービスを表示',
    'search_items'          => 'サービスを検索',
    'not_found'             => 'サービスが見つかりません',
    'not_found_in_trash'    => 'ゴミ箱にサービスはありません',
  );

  $args = array(
    'labels'              => $labels,
    'public'              => true,
    'publicly_queryable'  => true,
    'show_ui'             => true,
    'show_in_menu'        => true,
    'show_in_rest'        => true,  // ブロックエディタ対応
    'query_var'           => true,
    'rewrite'             => array( 'slug' => 'service', 'with_front' => false ),
    'capability_type'     => 'post',
    'has_archive'         => true,
    'hierarchical'        => false,
    'menu_position'       => 5,
    'menu_icon'           => 'dashicons-portfolio',
    'supports'            => array( 'title', 'editor', 'thumbnail', 'excerpt', 'custom-fields' ),
  );

  register_post_type( 'service', $args );
}
add_action( 'init', 'theme_register_service_post_type' );

/**
 * ニュース投稿タイプを登録（標準投稿を使わない場合）
 */
function theme_register_news_post_type() {
  $labels = array(
    'name'                  => 'お知らせ',
    'singular_name'         => 'お知らせ',
    'menu_name'             => 'お知らせ',
    'add_new'               => '新規追加',
    'add_new_item'          => '新規お知らせを追加',
    'edit_item'             => 'お知らせを編集',
  );

  $args = array(
    'labels'              => $labels,
    'public'              => true,
    'show_in_rest'        => true,
    'rewrite'             => array( 'slug' => 'news', 'with_front' => false ),
    'has_archive'         => true,
    'menu_position'       => 6,
    'menu_icon'           => 'dashicons-megaphone',
    'supports'            => array( 'title', 'editor', 'thumbnail', 'excerpt' ),
  );

  register_post_type( 'news', $args );
}
add_action( 'init', 'theme_register_news_post_type' );
```

### タクソノミー登録テンプレート（custom-taxonomies.php）
```php
<?php
/**
 * カスタムタクソノミーの登録
 *
 * @package ThemeName
 */

if ( ! defined( 'ABSPATH' ) ) {
  exit;
}

/**
 * サービスカテゴリータクソノミーを登録
 */
function theme_register_service_category() {
  $labels = array(
    'name'              => 'サービスカテゴリー',
    'singular_name'     => 'サービスカテゴリー',
    'search_items'      => 'カテゴリーを検索',
    'all_items'         => 'すべてのカテゴリー',
    'edit_item'         => 'カテゴリーを編集',
    'add_new_item'      => '新規カテゴリーを追加',
  );

  $args = array(
    'labels'            => $labels,
    'hierarchical'      => true,  // カテゴリー形式（false でタグ形式）
    'public'            => true,
    'show_in_rest'      => true,
    'rewrite'           => array( 'slug' => 'service-category' ),
  );

  register_taxonomy( 'service_category', array( 'service' ), $args );
}
add_action( 'init', 'theme_register_service_category' );
```

---

## 🔧 ACF（Advanced Custom Fields）

### フィールドグループ設計の考え方

1. **ページ単位のフィールドグループ**
   - `page_top_fields` - トップページ用
   - `page_about_fields` - 会社概要用

2. **投稿タイプ単位のフィールドグループ**
   - `service_fields` - サービス詳細用
   - `news_fields` - ニュース詳細用

3. **オプションページ（グローバル設定）**
   - `site_settings` - サイト全体設定
   - `footer_settings` - フッター設定

### よく使うフィールドタイプと用途
| フィールドタイプ | 用途 | 例 |
|-----------------|------|-----|
| テキスト | 短い文字列 | キャッチコピー |
| テキストエリア | 長い文字列 | 説明文 |
| WYSIWYG | リッチテキスト | 本文 |
| 画像 | 単一画像 | メインビジュアル |
| ギャラリー | 複数画像 | 施工事例写真 |
| リピーター | 繰り返しデータ | 特徴リスト、FAQ |
| グループ | 関連フィールドのまとめ | 住所情報 |
| 関連 | 他投稿との紐付け | 関連サービス |
| リンク | URLと設定 | CTAボタン |
| True/False | ON/OFF切り替え | 表示/非表示 |
| セレクト | 選択肢 | カテゴリ選択 |

### ACFフィールド取得の基本
```php
<?php
// 単一フィールド取得
$headline = get_field( 'hero_headline' );
$image = get_field( 'hero_image' );  // 配列で返る（ID, URL, alt等）

// リピーターフィールド
if ( have_rows( 'features' ) ) :
  while ( have_rows( 'features' ) ) : the_row();
    $title = get_sub_field( 'feature_title' );
    $desc = get_sub_field( 'feature_description' );
    $icon = get_sub_field( 'feature_icon' );
    ?>
    <div class="feature">
      <img src="<?php echo esc_url( $icon['url'] ); ?>" alt="">
      <h3><?php echo esc_html( $title ); ?></h3>
      <p><?php echo esc_html( $desc ); ?></p>
    </div>
    <?php
  endwhile;
endif;

// グループフィールド
$company = get_field( 'company_info' );
if ( $company ) :
  echo esc_html( $company['name'] );
  echo esc_html( $company['address'] );
endif;

// オプションページから取得
$phone = get_field( 'phone_number', 'option' );
$logo = get_field( 'site_logo', 'option' );
```

### ACFオプションページの登録
```php
<?php
/**
 * ACFオプションページを追加
 */
if ( function_exists( 'acf_add_options_page' ) ) {
  // メインオプションページ
  acf_add_options_page( array(
    'page_title'  => 'サイト設定',
    'menu_title'  => 'サイト設定',
    'menu_slug'   => 'site-settings',
    'capability'  => 'edit_posts',
    'redirect'    => false,
    'icon_url'    => 'dashicons-admin-generic',
    'position'    => 2,
  ) );

  // サブページ
  acf_add_options_sub_page( array(
    'page_title'  => 'ヘッダー設定',
    'menu_title'  => 'ヘッダー',
    'parent_slug' => 'site-settings',
  ) );

  acf_add_options_sub_page( array(
    'page_title'  => 'フッター設定',
    'menu_title'  => 'フッター',
    'parent_slug' => 'site-settings',
  ) );
}
```

---

## 🎠 動的要素の実装

### スライダー（Swiper.js推奨）
```php
// functions.php - スクリプト読み込み
function theme_enqueue_slider_scripts() {
  // Swiper CSS
  wp_enqueue_style(
    'swiper',
    'https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css',
    array(),
    '11.0.0'
  );
  
  // Swiper JS
  wp_enqueue_script(
    'swiper',
    'https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js',
    array(),
    '11.0.0',
    true
  );
  
  // 初期化スクリプト
  wp_enqueue_script(
    'theme-slider',
    get_template_directory_uri() . '/assets/js/slider.js',
    array( 'swiper' ),
    THEME_VERSION,
    true
  );
}
add_action( 'wp_enqueue_scripts', 'theme_enqueue_slider_scripts' );
```

```javascript
// assets/js/slider.js
document.addEventListener('DOMContentLoaded', function() {
  // メインビジュアルスライダー
  const heroSlider = new Swiper('.js-hero-slider', {
    loop: true,
    autoplay: {
      delay: 5000,
      disableOnInteraction: false,
    },
    pagination: {
      el: '.swiper-pagination',
      clickable: true,
    },
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
  });

  // カードスライダー（SP時のみ）
  const cardSlider = new Swiper('.js-card-slider', {
    slidesPerView: 1.2,
    spaceBetween: 16,
    breakpoints: {
      768: {
        slidesPerView: 2,
        spaceBetween: 24,
      },
      1024: {
        enabled: false,  // PC時は無効
      },
    },
  });
});
```

### 絞り込み検索（Ajax）
```php
// functions.php - Ajax ハンドラー
function theme_filter_posts() {
  // Nonceチェック
  check_ajax_referer( 'theme_filter_nonce', 'nonce' );

  $category = isset( $_POST['category'] ) ? sanitize_text_field( $_POST['category'] ) : '';
  
  $args = array(
    'post_type'      => 'service',
    'posts_per_page' => 12,
  );

  if ( $category ) {
    $args['tax_query'] = array(
      array(
        'taxonomy' => 'service_category',
        'field'    => 'slug',
        'terms'    => $category,
      ),
    );
  }

  $query = new WP_Query( $args );

  if ( $query->have_posts() ) :
    while ( $query->have_posts() ) : $query->the_post();
      get_template_part( 'template-parts/card', 'service' );
    endwhile;
    wp_reset_postdata();
  else :
    echo '<p class="no-results">該当するサービスがありません。</p>';
  endif;

  wp_die();
}
add_action( 'wp_ajax_filter_posts', 'theme_filter_posts' );
add_action( 'wp_ajax_nopriv_filter_posts', 'theme_filter_posts' );

// Ajaxの変数をJSに渡す
function theme_localize_scripts() {
  wp_localize_script( 'theme-main', 'themeAjax', array(
    'ajaxUrl' => admin_url( 'admin-ajax.php' ),
    'nonce'   => wp_create_nonce( 'theme_filter_nonce' ),
  ) );
}
add_action( 'wp_enqueue_scripts', 'theme_localize_scripts' );
```

```javascript
// assets/js/filter.js
jQuery(function($) {
  'use strict';

  const $filterButtons = $('.js-filter-button');
  const $resultsContainer = $('.js-results');

  $filterButtons.on('click', function(e) {
    e.preventDefault();
    
    const category = $(this).data('category');
    
    // アクティブ状態の切り替え
    $filterButtons.removeClass('is-active');
    $(this).addClass('is-active');

    // ローディング表示
    $resultsContainer.addClass('is-loading');

    $.ajax({
      url: themeAjax.ajaxUrl,
      type: 'POST',
      data: {
        action: 'filter_posts',
        nonce: themeAjax.nonce,
        category: category,
      },
      success: function(response) {
        $resultsContainer.html(response);
      },
      error: function() {
        $resultsContainer.html('<p>エラーが発生しました。</p>');
      },
      complete: function() {
        $resultsContainer.removeClass('is-loading');
      },
    });
  });
});
```

### お問い合わせフォーム（Contact Form 7 / MW WP Form）

#### Contact Form 7 カスタマイズ
```php
// functions.php
// Contact Form 7 のラッパークラス追加
add_filter( 'wpcf7_form_class_attr', function( $class ) {
  return $class . ' contact-form';
} );

// 送信完了後のリダイレクト
add_action( 'wp_footer', function() {
  if ( is_page( 'contact' ) ) : ?>
    <script>
      document.addEventListener('wpcf7mailsent', function(event) {
        location.href = '<?php echo home_url( '/contact/thanks/' ); ?>';
      }, false);
    </script>
  <?php endif;
} );
```

```scss
// _contact-form.scss
.wpcf7-form {
  .form-group {
    margin-bottom: $spacing-lg;
  }

  label {
    display: block;
    margin-bottom: $spacing-xs;
    font-weight: $font-weight-bold;
  }

  input[type="text"],
  input[type="email"],
  input[type="tel"],
  textarea {
    width: 100%;
    padding: $spacing-sm;
    border: 1px solid $color-border;
    border-radius: 4px;
    font-size: $font-size-base;
    
    &:focus {
      outline: none;
      border-color: $color-primary;
      box-shadow: 0 0 0 3px rgba($color-primary, 0.1);
    }
  }

  .wpcf7-submit {
    @extend .button;
    @extend .button--primary;
    cursor: pointer;
  }

  // バリデーションエラー
  .wpcf7-not-valid-tip {
    color: $color-error;
    font-size: $font-size-sm;
    margin-top: $spacing-xs;
  }

  // 送信中スピナー
  .wpcf7-spinner {
    margin-left: $spacing-sm;
  }
}
```

---

## 📋 実装チェックリスト

### CPT実装時
- [ ] `show_in_rest` を true に設定（ブロックエディタ対応）
- [ ] rewrite slug が他と被っていないか
- [ ] パーマリンク設定を再保存したか

### ACF実装時
- [ ] フィールド名は英語スネークケースか
- [ ] 出力時に適切なエスケープをしているか
- [ ] 空値チェックをしているか
- [ ] リピーターの最大件数を設定したか（必要に応じて）

### 動的要素実装時
- [ ] JSエラーが出ていないか
- [ ] SP/タブレット/PCで動作確認
- [ ] アクセシビリティ対応（キーボード操作、ARIA）
- [ ] ローディング状態の表示
